$kind: http-example
request:
  url: "{{baseUrl}}/transfers?status=cancelled"
  method: GET
  headers:
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    status: cancelled
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
