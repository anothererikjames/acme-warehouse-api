$kind: http-example
request:
  url: "{{baseUrl}}/alerts?severity=high&resolved=<boolean>"
  method: GET
  headers:
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    severity: high
    resolved: <boolean>
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
